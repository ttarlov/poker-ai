# PREVIEW-ONLY — do not merge to main.
# Runs the in-memory mock backend and enables the consensus animation
# preview (press "n" or load a session with ?nuke=1) on a throwaway deploy.
NEXT_PUBLIC_SUPABASE_URL=http://localhost:mock
NEXT_PUBLIC_SUPABASE_ANON_KEY=mock-anon-key
NEXT_PUBLIC_NUKE_PREVIEW=1
